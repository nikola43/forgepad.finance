# The repository root ignores `models/` (the ML model directory). That pattern
# also matches this package's `src/models/`, which would silently drop every
# response type from git — a fresh clone then fails to compile. Re-include it.
!src/models/

node_modules/
dist/
build/
*.tsbuildinfo
